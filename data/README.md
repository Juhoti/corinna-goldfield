# Data snapshots

Dated outputs of the analysis scripts, committed so the results can be used
without running the toolkit. Regenerate current versions with
`corinna_workflow.py` and `reach_analysis.py`; tenure columns reflect the
MRT tenement layer on the snapshot date only.
